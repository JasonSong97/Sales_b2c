# Utils

Pure domain-free helpers live here.

Examples:

- `cn.ts`
- date formatting helpers
- masking display helpers that do not know Admin API contracts

Helpers that call APIs or own singletons belong in `src/lib`.
