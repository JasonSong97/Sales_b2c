# Health Module

Current endpoint:

- `GET /api/health`

This module intentionally stays lightweight and does not use the full domain/application/infrastructure split until DB health checks are added.
